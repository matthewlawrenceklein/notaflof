
<div class="centering_div">
    <div class="artist_info">
        <h1>Artists</h1>
        <ul>
            <% @artists.each do |artist| %>
                <li>
                    <%= link_to "#{artist.name}  ||  #{artist.location}", artist_path(artist) %>
                </li>
            <% end %>
        </ul>
    </div>
</div>